# 行为契约 — [功能名称]
版本：v1.0 | 关联 PID：.pact/specs/[功能名]-pid.md | 日期：[日期]

> ⚠️ 体积控制：单文件建议不超过 15,000 字符（约 300 行）。
> 超出时 Claude Code 会自动截断为预览，导致 build / verify 阶段遗漏条目。
> 若功能过大，优先考虑拆分为子功能，而非压缩 FC 描述精度。

## 功能契约（FC）

### 正常路径（关键路径，必须测试）
FC-01：当 [操作] 时，系统应该 [结果]
FC-02：当 [操作] 时，系统应该 [结果]

### 边界与异常（关键路径，必须测试）
FC-03：当 [边界条件] 时，系统应该 [结果]
FC-04：当 [异常情况] 时，系统应该 [结果]
FC-05：当 [权限不足] 时，系统应该 [结果]

### 次要异常（标注[可选]，不生成自动化测试）
FC-06：[可选] 当 [低优先级异常] 时，系统应该 [结果]

## 非功能契约（NF）

### 可自动化测试
NF-01：当 [明确可触发的条件] 时，系统应该 [表现]

### 依赖环境（人工验收）
NF-02：[需要特殊环境才能测试，如慢网络]

## 测试覆盖范围
自动化：FC-01 ~ FC-0X，NF-01
人工验收：FC-0X（可选），NF-02（依赖环境）
