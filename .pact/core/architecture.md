# architecture.md — Architecture Spine 架构主干
> 冷层：按需读取，不在会话启动时强制加载。定义模块边界、实体归属、依赖方向和 ADR 触发条件。
> 最后更新：[日期]

## 架构原则
- [例如：业务状态变化由 owning module 负责]
- [例如：跨模块访问通过公开接口，不直接写入他方实体]

## 目录结构
```
[由 /pact.init 生成]
```

## 模块边界
| 模块 | 路径 | 职责 | 不负责 |
|------|------|------|--------|
| [模块名] | src/[路径] | [职责] | [边界外职责] |

## 核心实体归属
| 实体 | Owner 模块 | 写入入口 | 备注 |
|------|------------|----------|------|
| [实体] | [模块] | [Service/API] | [说明] |

## 状态机归属
| 状态机 | Owner 模块 | 状态值 | 变更规则 |
|--------|------------|--------|----------|
| [状态机] | [模块] | [状态A / 状态B] | [规则] |

## 权限判断位置
| 场景 | 判断层 | 规则来源 | 失败表现 |
|------|--------|----------|----------|
| [场景] | [路由/服务/数据库] | [规则] | [表现] |

## 数据写入边界
- [实体] 只能通过 [模块/服务] 写入。
- 跨模块更新需要 [编排位置/事务策略]。

## 依赖方向
- 允许：[模块A] → [模块B]
- 禁止：[模块B] → [模块A]
- 共享逻辑放置：[shared / common / core]

## 外部依赖
| 库名 | 用途 | 版本 | 引入日期 |
|------|------|------|---------|
| [库名] | [用途] | [版本] | [日期] |

## ADR 触发条件
命中以下任一情况时，在 `.pact/knowledge/decisions/` 记录架构决策：
- 新增顶级模块。
- 新增关键外部依赖。
- 改变核心实体关系。
- 改变状态机。
- 改变权限模型。
- 引入异步任务、缓存、事务边界或外部集成。
- 同一问题存在两个以上合理技术方案。

## 架构决策
| 日期 | 决策 | 原因 |
|------|------|------|
| [日期] | [选择了什么] | [为什么] |
